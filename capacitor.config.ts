import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dp_warehouse.app',
  appName: 'DpWarehouseSystem',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
